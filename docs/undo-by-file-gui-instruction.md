Update the Text History “By file” view so it behaves like search results by file: show a compact list of affected files first, with each file row displaying the file label/path and a change count, and make each row expandable/collapsible to reveal all undo history changes for that file underneath in chronological order; the expanded changes should keep the existing undo/redo click behavior and visual state, while the collapsed file list should let users quickly scan which files are affected without being forced to scroll through every individual change.
